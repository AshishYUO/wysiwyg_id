export default keyEvents = {
    Control: {
        Shift: {
            // Redo event
            Z: event => {

            }
        },
        // Select all events
        A: event => {

        },
        // Undo event
        Z: event => {

        }
    },
    // Enter event
    Enter: event => {
        
    }
}
