{
    "name": "wysiwyg_id",
    "version": "1.0.0",
    "description": "A simple web word editor (In development).",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "npm run",
        "dev": "bun build --watch --outdir=dist/ ./src/index.ts",
        "build": "bun build --minify --outdir=dist/ ./src/index.ts"
    },
    "keywords": [
        "word",
        "editor"
    ],
    "author": "Ashish Daulatabad",
    "license": "ISC",
    "dependencies": {
        "css-loader": "^5.2.4",
        "style-loader": "^2.0.0",
        "typescript": "^4.6.2"
    },
    "devDependencies": {
        "bun-types": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}